-- DELETE

-- Deleta todos os registros da tabela `reservas` onde o status é 'cancelada'
DELETE FROM reservas WHERE status = 'cancelada';

-- Deleta todos os registros da tabela `reservas`
DELETE FROM reservas;
